import 'package:flutter/material.dart';

class QnA extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("QnA 페이지", textScaleFactor: 2,),),
    );
  }
}
