import 'package:flutter/material.dart';

class PersonalAuth extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("PersonalAuth", textScaleFactor: 2,),
      ),
    );
  }
}
